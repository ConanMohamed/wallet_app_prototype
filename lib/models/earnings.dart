import 'package:flutter/material.dart';

class EarningsInfo {
  String mainIcon;
  String title;
  String amount;
  Color color;
  EarningsInfo({
    required this.mainIcon,
    required this.title,
    required this.amount,
    required this.color,
  });
}